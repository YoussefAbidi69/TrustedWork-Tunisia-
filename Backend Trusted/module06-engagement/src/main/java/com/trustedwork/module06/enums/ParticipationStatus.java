package com.trustedwork.module06.enums;

public enum ParticipationStatus {
    JOINED,    // User has started the mission
    SUCCESS,   // User has completed the tasks but not yet claimed the reward
    CLAIMED    // User has claimed the XP
}
